class Greet(message) =
  let str () =
    "Hello, " ^ message ^ " world!"

class SayTwice(message) : Greet(message) 
  let str () =
    super.str() ^ super.str()
  
let gt = SayTwice("new")
print(gt.str())
