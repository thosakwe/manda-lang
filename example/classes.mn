class Greet {
  new(val message: String)

  fn str() ->
    "Hello, " + message + "world"
}

class SayTwice : Greet {
  new(message) : super(message)

  fn str() ->
    super.str() + super.str()
}
  
fn main {
  var gt = SayTwice("new")
  print(gt.str())
}

